/// <reference types="cypress" />

describe('header content', () => {
  beforeEach(() => {
    cy.visit('http://localhost:8881/')
  })

  specify('', () => {

  })

  specify('', () => {
    
  })

})