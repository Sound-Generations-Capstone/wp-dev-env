/// <reference types="cypress" />

describe('header content', () => {
  beforeEach(() => {
    const url = Cypress.env('SITE_URL') || 'http://localhost:8881/';
    cy.visit(url)
  })

  specify('', () => {

  })

  specify('', () => {
    
  })

})