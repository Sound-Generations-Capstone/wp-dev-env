/// <reference types="cypress" />

describe('header content', () => {
  beforeEach(() => {
    const url = Cypress.env('SITE_URL');
    cy.visit(url)
  })

  specify('', () => {

  })

  specify('', () => {
    
  })

})