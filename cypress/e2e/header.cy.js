/// <reference types="cypress" />

describe('header content', () => {
  beforeEach(() => {
    cy.visit('http://localhost:8882/')
  })

  specify('', () => {

  })

  specify('', () => {
    
  })

})